# 桌面便签功能说明

对应当前发布版本 **0.3.20**（`desktop-note-0.3.20-x64`）。本文记录程序实际具备的能力与行为。

程序是 Windows 本地桌面应用。待办、暂存和窗口状态都保存在本机，不联网、不上传。

## 工作区

程序内有两个工作区，可来回切换，上次打开的工作区会记住：

- **待办**：记录事项正文，并可附带时间。
- **暂存**：存放临时文字和图片，供复制、另存或查看。

标题栏左侧显示当天日期（例如 `8月12日 周三`），大约每分钟刷新一次。中间切换待办 / 暂存；右侧为置顶、设置、最小化、关闭。设置按钮使用齿轮图标。

底部输入框可直接输入待办，回车添加；也可用加号按钮或 `Ctrl + N`。

## 待办

- 可新建、就地编辑、自动保存。
- 单条正文最多 1000 字；待办总数最多 100 条。
- 空内容待办删除时立即移除；有内容的待办需在约 2 秒内再次确认才会删除。
- 可用拖动手柄调整顺序，也可用 `Alt + ↑` / `Alt + ↓` 移动当前项。
- 每条待办左侧有复选框。勾选后该项记为完成：正文划掉，并沉到列表底部。可取消勾选，恢复为未完成并回到未完成项之后。
- 可为每条待办设置时间，三种模式：
  - **具体时间**：日期 + 时刻（精确到分钟）
  - **仅日期**
  - **不设置**
- 时刻可用 `14:44`、`14：44` 或 `1444` 这类输入，程序会规范成 `HH:mm`。
- 时间只作为待办上的标记保存，**不会**弹出提醒或闹钟。
- 启动时会读取当前存储；若只有更早版本的数据，会迁移为当前待办格式。

## 暂存

- 可保存文字或图片。文字最多 5000 字；暂存最多 200 项；单张图片不超过 30MB。
- 支持的图片格式：PNG、JPEG、GIF、WebP、BMP。实际导入时按文件内容识别，而不是只看扩展名。
- 写入途径：
  - 选择本地文件（可一次多选，单次最多 20 个）
  - 把图片文件拖入窗口
  - 粘贴：优先文件路径，其次剪贴板位图，再次剪贴板文字
- 文字可在列表中直接改，输入后会延迟写入磁盘；失焦或退出前会补一次保存。
- 图片会保存原图，并生成缩略图供列表使用。
- 可复制到系统剪贴板：文字复制为文本，图片复制为位图。
- 可另存到用户指定路径：图片按原格式导出；文字另存为 `.txt`。
- 删除单项需在约 2 秒内再次确认；清空全部暂存需再次确认。
- 顺序可用拖动手柄或 `Alt + ↑` / `Alt + ↓` 调整。
- 右键菜单：
  - 图片：预览、复制、另存、删除
  - 文字：预览、复制、另存、删除

## 预览

- 光标停在暂存项的图标/摘要上时，会在主窗口旁打开**悬浮预览**（不抢焦点）。移开后关闭；移到预览窗口上会继续保持。
- 点击该项会打开**独立预览窗口**。独立窗口打开期间，主窗口不会因为光标离开而自动贴边收起。
- 在悬浮预览上再点一次，会改为打开独立预览窗口。
- 独立预览窗口中可以复制、另存；`Esc` 关闭；`Ctrl + C` 复制；`Ctrl + S` 另存。
- 主窗口贴在屏幕边缘且尚未展开时，不打开悬浮预览。
- 隐藏到托盘、贴边收起、退出程序时，会关掉已打开的预览窗口。

## 外观

设置里可切换六款皮肤，布局相同、配色与局部交互语言不同：

| 皮肤 | 说明 |
| --- | --- |
| 中性灰 | 浅底、蓝色强调；默认皮肤 |
| 暖白纸 | 奶油纸色、橄榄强调 |
| 石墨 | 深灰底、电蓝强调 |
| 雾面 | 深色磨砂玻璃 |
| 编辑 | 黑白排版、下划线标签 |
| 侘寂 | 砂岩底、颗粒质感 |

旧版四款皮肤会自动对应到新皮肤：素白 → 中性灰，黑曜 → 石墨，烟灰 → 雾面，经典黄 → 暖白纸。

窗口背景透明度可在 50%–100% 之间调节，默认 92%。皮肤和透明度会随待办一起保存在本机。

## 窗口与系统行为

- 不在任务栏占用按钮；关闭窗口默认是隐藏，而不是退出。退出需通过托盘菜单。
- 可拖动窗口。靠近屏幕四边约 30 像素内松手后，会吸附到该边并收成一条可唤出的边缘。
- 光标停在贴边露出的部位约 120ms 后展开；离开约 220ms 后重新收起。也可点边缘把手把窗口彻底拉回屏幕内。从贴边位置拖开足够距离会取消吸附。
- 可始终置顶（默认开启），该状态会保存。
- 默认窗口 **420×480**。宽 320–640、高 280–640，可在设置里修改并保存。若上次保存的是旧版 300×200 或 330×230，启动时会改用新的默认尺寸。
- 可选开机自启动，默认关闭；写入当前 Windows 用户的启动项。便携版会指向正在使用的可执行文件。
- 系统托盘：单击显示/隐藏；菜单可打开或隐藏窗口、新建待办、切换置顶、退出。
- 全局快捷键 `Ctrl + Shift + Space`：显示或隐藏主窗口。若被其他程序占用，托盘菜单会标明不可用。

## 快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl + Shift + Space` | 全局显示或隐藏主窗口 |
| `Ctrl + N` | 待办工作区新建待办；暂存工作区新建一条空文字 |
| `Ctrl + Enter` | 在待办编辑框中再新建一条 |
| `Alt + ↑` / `Alt + ↓` | 调整当前待办或暂存项顺序 |
| `Ctrl + V` | 不在文本框内时，把剪贴板内容写入暂存 |
| `Esc` | 关闭设置、时间面板，或关闭独立预览 |
| `Ctrl + C` / `Ctrl + S` | 仅独立预览窗口：复制 / 另存 |

## 数据位置与范围

| 内容 | 位置 |
| --- | --- |
| 待办正文、完成状态、顺序、当前工作区、皮肤、透明度 | Electron 本地存储键 `desktop-notes:v3` |
| 暂存索引、原图、缩略图 | 应用 `userData/staging` |
| 窗口位置、大小、贴边、置顶 | 应用 `userData/window-state.json` |
| 开机自启动 | Windows 当前用户启动项 |

清理应用数据前，需要先自行备份待办和暂存文件。程序没有账号、同步和云备份。
