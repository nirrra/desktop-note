# Task Plan: 桌面卡通便利条

## Goal
创建一个默认 300×200 的高级半透明桌面事项组件：统一采用 B 版紧凑时间列表布局，支持四套皮肤、滚动、拖拽排序、三种时间精度，并可在设置中调整尺寸、皮肤与透明度。

## Current Phase
Phase 25

## Phases

### Phase 1: Requirements & Discovery
- [x] 理解用户希望参考 Codex 宠物式悬浮显示
- [x] 确认当前工作区为空，需要从零搭建
- [x] 记录第一版范围与合理假设
- **Status:** complete

### Phase 2: Architecture & Visual Direction
- [x] 确定桌面容器、窗口行为和状态存储方式
- [x] 定义卡通视觉语言与展开/收起交互
- [x] 创建项目结构和运行脚本
- **Status:** complete

### Phase 3: Implementation
- [x] 实现透明、无边框、置顶的桌面窗口
- [x] 实现卡通收起态、便利条、拖动与动效
- [x] 实现便签、专注计时、常用短语等核心工具
- [x] 实现系统托盘、全局快捷键和隐藏/唤回
- **Status:** complete

### Phase 4: Testing & Visual QA
- [x] 执行静态检查和自动化交互测试
- [x] 启动应用并截图检查视觉与窗口行为
- [x] 修复发现的问题
- **Status:** complete

### Phase 5: Delivery
- [x] 补充中文 README 与操作说明
- [x] 确认所有交付文件和启动方式
- [x] 向用户交付可运行版本
- **Status:** complete

### Phase 6: V2 Scope & Simplification
- [x] 将产品范围收敛为多条便签
- [x] 定义就地编辑、自动保存与删除行为
- [x] 定义四边自动吸附隐藏与恢复行为
- **Status:** complete

### Phase 7: V2 Implementation
- [x] 移除专注、常用语和展开/收起双态
- [x] 实现多条便签的数据迁移、新建、直接编辑与删除
- [x] 实现窗口四边检测、自动隐藏和边缘拉手恢复
- [x] 重做清晰字体与低噪音卡通界面
- **Status:** complete

### Phase 8: V2 Testing & Visual QA
- [x] 更新静态与功能测试
- [x] 验证多便签编辑、持久化、删除和空状态
- [x] 验证四边隐藏/恢复及真实窗口截图
- **Status:** complete

### Phase 9: V2 Packaging & Delivery
- [x] 更新 README 与版本号
- [x] 生成并复验 Windows x64 便携版
- [x] 记录新产物校验值并交付
- **Status:** complete

### Phase 10: V3 Interaction & Visual System
- [x] 将 A/B/C 设计方向映射为同布局皮肤
- [x] 增加经典黄色便签纸皮肤
- [x] 定义精确到分钟、仅日期与无时间三种模式
- [x] 定义滚动、拖拽优先级和设置面板
- **Status:** complete

### Phase 11: V3 Implementation
- [x] 将默认窗口改为 300×200，并支持自定义宽高
- [x] 实现统一 B 布局与四套可切换皮肤
- [x] 实现事项滚动、直接编辑、拖拽排序和优先级持久化
- [x] 实现三种时间模式及时间编辑器
- [x] 实现尺寸、皮肤、透明度设置和 V2 数据迁移
- [x] 移除全部“小芽”品牌与图标元素
- **Status:** complete

### Phase 12: V3 Testing & Visual QA
- [x] 更新静态检查与功能 QA
- [x] 验证四皮肤布局一致、透明度和尺寸调整
- [x] 验证滚动、拖拽排序和三种时间模式
- [x] 验证动态尺寸下的四边隐藏/恢复
- **Status:** complete

### Phase 13: V3 Packaging & Delivery
- [x] 更新产品名、图标、README 和版本号
- [x] 生成 Windows x64 便携版并复验
- [x] 记录 SHA-256 并交付
- **Status:** complete

### Phase 14: V3.1 Material Calibration
- [x] 分析用户提供的奶黄色便签参考图
- [x] 统一四皮肤的透明度数值与材质 alpha 映射
- [x] 将经典黄调整为低饱和奶油柠檬黄
- [x] 复测四皮肤同透明度、同尺寸和完整功能
- [x] 重新生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 15: V3.2 Typography & Control Polish
- [x] 使用 `frontend-design` 审查当前 300×200 成品视觉
- [x] 优化离线字体栈、字号、字重与数字排版
- [x] 让单行/多行事项在行内按内容高度垂直居中
- [x] 将无时间横杠改为清晰的时钟胶囊，并统一图标笔画
- [x] 复测四皮肤布局、尺寸、编辑、时间和完整功能
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 16: V3.3 Reference-Led Redesign & Right-Edge Repair
- [x] 使用 `frontend-design` 分析四张用户示意图与当前成品差异
- [x] 定位右边缘越界后无法触发隐藏的真实路径
- [x] 按参考比例重做标题、正文、按钮、时间卡片、边框与材质
- [x] 修复右边缘越界与 frameless moved 事件遗漏
- [x] 增加真实手动越界路径和四皮肤几何回归 QA
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 17: V3.4 Item Typography Calibration
- [x] 复核当前事项正文与 300×200 信息密度
- [x] 将事项正文由 14px 调整为 13px，并保持垂直居中和两行上限
- [x] 更新字体回归断言并复测四皮肤完整功能
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 18: V3.5 Pale Milk-Yellow Calibration
- [x] 复核当前经典黄主材质与用户“浅奶黄”反馈
- [x] 降低经典黄饱和度并提高明度，统一关联色阶与设置色板
- [x] 增加经典黄精确色值回归并复测完整功能
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 19: V3.6 Integrated Time Metadata
- [x] 基于确认稿锁定事项/时间字号与纯文本方向
- [x] 移除时间卡片、底色、边框、图标与二级标签
- [x] 将日期改为补零 `MM/DD`，分钟时间保持 `HH:mm`，空时间静态留白
- [x] 增加纯文本时间、四皮肤同布局与交互可用性回归
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 20: V3.7 Direct Time Entry & Hover Reveal
- [x] 将精确时间拆为日历日期 + 可直接键入的时间文本框
- [x] 支持 `14:44`、`1444` 等输入规范化并校验 00:00–23:59
- [x] 实现侧边拉手 hover 临时展开、离开窗口自动收回、点击转为常驻展开
- [x] 增加输入、悬浮展开/收回和既有四边隐藏回归 QA
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 21: V3.8 Real Pointer Hover Repair
- [x] 用真实系统鼠标进入侧边拉手复现 0.3.7 失效路径
- [x] 查明渲染层 hover 事件未形成可用展开体验的根因
- [x] 实现可靠的侧边悬浮展开、移出收回与完整 UI 重绘
- [x] 增加真实指针路径 QA，避免再次只测内部状态函数
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 22: V3.9 Center-Facing Edge Handle
- [x] 复核四个折叠方向的屏幕边缘与桌面中央朝向
- [x] 将贴屏侧改为平直、朝桌面中央侧改为圆角
- [x] 增加四边圆角与边框方向的运行时回归断言
- [x] 目视复核四边折叠截图并回归 hover 展开
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 23: V3.10 Undock by Dragging
- [x] 复现并定位折叠拉手拖离边缘后回弹的状态路径
- [x] 从折叠态开始拖动时解除原停靠并恢复完整组件
- [x] 保证松手位置停留，只有靠近任一边缘时才重新折叠
- [x] 增加拖离四边与重新停靠的自动化回归
- [x] 完成源码及 Windows x64 便携版验收
- **Status:** complete

### Phase 24: V3.11 Daily Date Header
- [x] 将主标题由“便签”替换为本地当日日期 `YYYY/MM/DD`
- [x] 调整日期字号、字重与数字间距，保持 300×200 标题栏平衡
- [x] 支持跨午夜自动刷新并增加时区安全的格式回归
- [x] 完成四皮肤源码截图与完整功能回归
- [x] 生成并复验 Windows x64 便携版
- **Status:** complete

### Phase 25: V3.12 Faster Startup
- [x] 建立 portable 与已解压程序的启动耗时基准
- [x] 定位自解压与应用初始化各自占用的时间
- [x] 提供免重复解压的快速启动成品并保留便携选择
- [x] 优化可安全延后的主进程初始化工作
- [x] 修复设置/时间浮层在重复侧边收起后残留的问题
- [x] 增加多轮展开收起与浮层清理回归
- [x] 消除 portable/ZIP 子进程标准输出断开导致的 EPIPE 弹窗
- [x] 复测启动耗时、完整功能与 Windows x64 成品
- **Status:** complete

### Phase 26: V3.13 Optional Launch at Login
- [x] 接入 Windows 开机自启动系统状态，默认关闭
- [x] 在设置面板加入与现有视觉一致的自启动开关
- [x] 实现状态读取、切换、失败回滚与用户反馈
- [x] 增加静态与 Electron 功能回归，避免测试污染真实启动项
- [x] 构建并复验 Windows x64 ZIP 与单文件成品
- **Status:** complete

### Phase 27: V3.14 Launch-at-login Verification Fix
- [x] 只读核对 Windows Run 与 StartupApproved 的实际写入状态
- [x] 修复启动项写入成功却被立即校验误判的问题
- [x] 覆盖系统状态滞后、任务管理器禁用与 portable 路径场景
- [x] 复验设置开关、错误回滚及既有完整功能
- [x] 构建并验证 Windows x64 ZIP 与单文件成品
- **Status:** complete

### Phase 28: V3.15 Settings Reverse-scroll Fix
- [x] 复现设置内容滚到底后无法返回顶部的状态
- [x] 定位滚动容器、滚轮事件与面板重开状态之间的根因
- [x] 修复向上滚动并为面板重开提供确定的初始位置
- [x] 增加底部 → 顶部 → 关闭重开的 Electron 回归
- [x] 构建并验证 Windows x64 ZIP 与单文件成品
- **Status:** complete

### Phase 29: V3.16 Staging Workspace
- [x] 盘点现有 renderer、主进程 IPC、持久化与 QA 架构
- [x] 建立暂存文件索引、原图/缩略图存储和安全文件操作 IPC
- [x] 实现“事项 / 暂存”同尺寸工作区与暂存项拖动排序
- [x] 实现剪贴板智能识别、文件选择、拖放和文本暂存
- [x] 实现图片预览、复制、另存、删除和清空确认
- [x] 增加迁移、异常恢复、边缘折叠与既有功能回归
- [x] 构建并验证 Windows x64 ZIP 与单文件成品
- **Status:** complete

### Phase 30: V3.17 Floating Preview & Interaction Polish
- [x] 移除标题栏左上装饰点并回收空间，保留日期区域拖动能力
- [x] 将所有用户可见“事项”统一改为“待办”
- [x] 空待办首次点击删除即移除，非空待办继续二次确认
- [x] 将受限的组件内图片预览替换为可超出 300×200 的独立悬浮预览窗
- [x] 为暂存图片/文字加入原生右键菜单及复制、另存、预览、删除操作
- [x] 增加悬浮预览尺寸、右键动作、空待办删除与既有边缘行为回归
- [x] 构建并验证 Windows x64 ZIP 与单文件成品
- **Status:** complete

## Key Questions
1. 如何在收起时像桌面宠物一样轻巧，同时展开后能承载真正有用的工具？
2. 如何在不引入不必要依赖的前提下实现透明窗口、托盘、全局唤回和持久化？
3. 第一版应提供哪些默认工具，既能体现便利条价值又保持界面简洁？
4. 多条便签如何做到无需进入“编辑模式”，点击正文即可直接修改？
5. 窗口贴近任一工作区边缘时，如何自动藏起但保留稳定、易点击的恢复拉手？
6. 四套材质如何在不改变布局的前提下保持辨识度与一致的可读性？
7. 300×200 内如何同时容纳编辑、时间、排序和设置，而不过度堆叠控件？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 以 Windows 桌面为首要运行环境 | 当前工作区与 shell 均为 Windows，最贴近用户实际使用场景 |
| 第一版采用 Electron 容器 | 原生支持透明无边框窗口、置顶、托盘、全局快捷键与丰富 CSS 动效 |
| 卡通角色使用 HTML/CSS/SVG 绘制 | 保持清晰、轻量、可动画且无需外部图片版权或资源依赖 |
| 默认工具为便签、专注计时、常用短语 | 覆盖快速记录、时间管理和高频复制三个桌面便利场景 |
| 打包为 Windows x64 便携版 | 用户无需安装即可双击运行，同时保留源码便于继续迭代 |
| V2 删除专注与常用语 | 用户明确只需要便签，降低界面和认知负担 |
| 每条便签使用自适应高度 textarea | 点击即可编辑，键盘行为自然，且能保持纯文本安全存储 |
| 支持左、右、上、下四边自动隐藏 | “靠近桌面边缘”按完整桌面行为实现，而不限定某一侧 |
| 隐藏后保留 36px 卡通拉手 | 兼顾真正隐藏与可发现、可点击恢复 |
| V3 统一采用 B 版紧凑列表结构 | 每项正文在左、时间在右，最适合 300×200 和优先级浏览 |
| 四皮肤只改变材质、颜色、边框与阴影 | 保证切换皮肤后交互位置和信息密度完全一致 |
| 默认黑曜石皮肤 | 用户明确选择 B 的布局；深色玻璃在 2K 桌面上辨识度高 |
| 时间分为精确、日期、无时间 | 分别对应 datetime-local、date 和空值，覆盖用户提出的三种精度 |
| 列表顺序即优先级 | 拖动左侧把手上下移动，排序即时持久化；不增加额外优先级字段 |
| 尺寸设置使用宽高数值输入 | 默认 300×200，可在安全范围内按 10px 调整，比固定预设更灵活 |
| V3 产品名改为“桌面便签” | 彻底移除“小芽”品牌与吉祥物元素 |
| V3.1 四皮肤使用同一透明度映射 | 相同滑块数值应产生一致的材质 alpha；皮肤差异只来自颜色与质感 |
| 经典黄改为奶油柠檬黄 | 参考图为低饱和、偏白的浅黄色纸张，避免现有金橙色过于强烈 |
| V3.2 采用精致工具型排版 | 维持克制与信息密度，通过 13px 正文、温和字重和更精确的垂直节奏提升观感 |
| 文本域按实际内容高度居中 | 单行保持行中居中，多行最多两行并在 42px 编辑区内自然增长，避免固定两行造成上贴 |
| 无时间状态使用时钟胶囊 | 图形和“时间”文字比横杠更明确，并与已有时间状态复用相同 66px 控件尺寸 |
| V3.3 采用参考图的“大标题 + 主按钮 + 时间卡片”比例 | 300×200 不变，通过内部比例而非增加窗口尺寸建立层级 |
| 删除控件不再占据常态视觉焦点 | 仅在 hover/focus 时出现，参考图主界面保持纯净 |
| 边缘检测同时使用指针与窗口 bounds | 解决 frameless 窗口被拖过右边界超过阈值后绝对距离反而变大的问题 |
| `move` 停止后增加 debounce 兜底 | 某些 Windows/Electron frameless 拖动不稳定触发 `moved`，仍能完成靠边判断 |
| V3.4 事项正文回调至 13px | 用户明确认为 14px 偏大；标题与时间信息保持现有层级，仅降低可编辑正文的视觉重量 |
| V3.5 经典黄调整为浅奶油白黄 | 将主材质由 `rgb(246,240,169)` 提亮并显著收窄 RGB 色差，降低黄色感但保留与雾白皮肤的可辨识度 |
| V3.6 时间采用行内元数据 | 事项约 12.5px、时间约 12px；无框无图标，仅靠字重、色阶和右对齐建立层级，使整行成为一个完整信息单元 |
| 空时间默认静态留白、悬停显现提示 | 保持确认稿的纯净构图，同时保留足够点击热区和设置时间的可发现性 |
| V3.7 精确时间使用日期控件 + 文本时间 | 日期保留系统日历；时间避免滚轮选择，允许键盘直接录入并统一为 `HH:mm` |
| 边缘 hover 采用临时预览态 | 保留 dockedEdge，不夺取焦点；离开整个展开窗口后收起，点击 UI 时升级为正常展开以支持编辑 |
| V3.8 hover 由主进程系统光标状态机负责 | DOM enter/leave 会与 BrowserWindow 位移竞争；系统坐标轮询配合驻留延迟可跨 Windows 透明窗口移动稳定工作 |
| 展开时一帧级重建透明窗口表面 | Chromium 状态正确但 DWM 可能保留旧透明表面；hide/move/showInactive 能无焦点地强制完整 UI 重新合成 |
| 折叠拉手圆弧统一朝桌面中央 | 贴屏侧保持平直可形成自然的屏幕切边，向内侧圆润才像从桌面边缘伸出的把手 |
| 折叠拉手拖动代表主动解除停靠 | 用户把拉手拖离边缘时意图是重新摆放；应先恢复完整组件再跟随拖动，不能由旧 `dockedEdge` 把窗口吸回原边缘 |
| 标题日期使用本地日期分量 | `getFullYear/getMonth/getDate` 不受 UTC 日期切换影响，适合显示用户所在时区的“当日”；应用常驻时每分钟检查一次以跨午夜更新 |
| 启动优化先测 portable 与 unpacked 差值 | 当前单文件 portable 每次都要从约 90MB 可执行文件解出完整 Electron；若解压占主导，安装版或预解压快速版比微调几毫秒 JS 更有效 |
| 进入真正折叠态时关闭全部临时浮层 | 设置/时间面板不属于持久内容；折叠后保留它们既会泄漏到屏幕边缘，也不符合用户下一次展开的预期 |
| 开机自启动由主进程通过 Electron login item API 管理 | 这是 Windows 系统级状态；renderer 仅负责展示与请求，避免把不可靠的副本写进便签 localStorage |
| Windows 自启动状态以精确匹配的 `launchItems[].enabled` 为准 | Electron 43 对自定义启动项 name 的 `openAtLogin` 会持续假阴性；名称、路径、参数精确匹配后读取 StartupApproved 状态才能同时支持开启与任务管理器禁用 |
| 设置外层使用 `overflow:clip`，只有 `.settings-content` 可滚动 | `overflow:hidden` 外层仍会因底部控件聚焦发生程序化滚动，造成标题被卷走且滚轮无法控制；clip 从布局层消除错误滚动容器 |
| 暂存工作区沿用第一版示意图并保留排序手柄 | 用户明确选择带手柄版本；新内容默认置顶，同时允许拖动形成用户自定义顺序 |
| 暂存数据使用 userData 文件存储 | 图片不写入 localStorage/base64；索引、原图和缩略图独立保存，便于预览、复制、另存与容量控制 |
| 粘贴只在非编辑状态触发智能暂存 | 编辑事项或暂存文字时维持原生粘贴；其余状态按图片优先、文件图片其次、纯文本最后自动建项 |
| 图片预览改为独立悬浮 BrowserWindow | 300×200 主组件无法同时兼顾列表与长图可读性；预览窗按图片比例扩大并限制在当前显示器工作区内，可完整缩放展示且不改变组件尺寸 |
| 暂存右键操作使用原生系统菜单 | 符合 Windows 桌面习惯，图片提供悬浮预览/复制/另存/删除，文字提供复制/删除；操作仍复用受控主进程存储能力 |
| 顶部日期区域本身作为拖动热区 | 移除没有独立功能的六点装饰后仍保持无边框窗口可拖动，并为“待办/暂存”导航释放横向空间 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 0.3.12 成品 QA 子进程在宿主管道关闭后写 `QA_CAPTURE`，触发 `EPIPE: broken pipe` 主进程弹窗 | 1 | 删除运行时 QA 的 stdout/stderr 输出，并为 GUI 进程标准流安装 EPIPE 防崩溃处理；重新构建后验证宿主提前退出也不弹窗 |
| V3.13 首轮源码 QA 通过 `Start-Process electron.cmd` 启动后未生成结果文件 | 1 | 启动器提前退出且测试 PID 已结束；改用当前 shell 直接运行 Electron，以保留退出码和诊断输出，不重复原启动方式 |
| V3.13 直接运行 Electron QA 时 GPU 子进程以 `-1073741515` 连续退出 | 2 | `--disable-gpu` 在受限沙箱内仍触发图形子进程崩溃；按 GUI 测试规范改到受控沙箱外运行，并继续使用隔离 userData 与模拟启动项 |
| V3.13 首次打包连接 Electron 构建资源时返回 `EACCES 198.18.0.16:443` | 1 | 属于网络沙箱限制；按权限规范以受控联网权限重试同一 `pnpm dist` 构建 |
| V3.13 最终源码 QA 汇总命令把 `$error` 当作路径变量 | 1 | PowerShell 的 `$Error` 不区分大小写且为只读自动变量；结果文件已正常通过，后续汇总统一改用 `$qaErrorPath` |
| V3.15 原生滚轮回归未发送足够的向上刻度 | 2 | 首轮 ZIP 剩 0.8px，第二轮源码剩 2.4px；每刻度约移动 26.4px，测试从 108px 只发 4 次便提前测量。改为 8 次并等待 160ms 结算，顶部容差仍保持 ≤1px |
| V3.16 首轮静态检查仍断言旧 `.title-copy time` 结构 | 1 | 新标题已改为 `.drag-surface > time` 并加入工作区导航；更新测试为新 DOM/CSS，同时保留 14px 日期与 tabular-nums 可读性断言 |
| V3.16 首次打包连接 Electron 构建资源时返回 `EACCES 198.18.0.111:443` | 1 | 属于网络沙箱限制；使用受控联网权限重试同一 `pnpm dist`，不更改代码或构建配置 |
| V3.12 计划记录的多文件补丁包含空 hunk | 2 | 此后该阶段的计划更新只使用单文件补丁；产品代码未受影响 |
| V3.11 计划进度的多文件补丁含空 hunk 而未解析 | 2 | 后续完全禁止在该模式下组合多文件补丁，只提交单文件补丁；产品代码未受影响 |
| V3.10 Computer Use 首次拖动或刷新返回结果未知 | 1 | 丢弃旧截图与坐标，按 Computer Use 规范重新观察唯一隔离窗口后再决定是否重试 |
| V3.10 Computer Use 无法向透明测试窗口注入拖动 | 3 | 最终错误确认目标坐标实际被 `ChatGPT.exe` 窗口覆盖；按安全规则不控制 Codex/ChatGPT UI，停止重试并保留 Electron 原生 move/moved 自动回归作为验收证据 |
| 隔离 Electron 测试进程清理命令因“进程已不存在”返回状态 1 | 1 | 使用只读条件复核 PID 8552，确认测试主进程已停止；未影响用户正在运行的旧版便签 |
| V3.10 进度补丁包含多余 hunk 分隔而未解析 | 2 | 不再组合多文件 hunk，改为三个独立补丁；产品代码未受影响 |
| V3.10 计划补丁因现有错误行的中文空格不一致而未匹配 | 1 | 拆分补丁并只使用稳定的章节上下文；产品代码未受影响 |
| PowerShell 找不到全局 `python`，会话恢复脚本未启动 | 1 | 改用 Codex 工作区自带 Python 路径，恢复检查成功且无遗留上下文 |
| 沙箱阻止访问 npm registry（EACCES） | 1 | 按权限规范改为经批准的网络安装，依赖成功下载 |
| pnpm 11 因 `electron-winstaller` 构建脚本未批准而以状态 1 结束 | 1 | 在 `pnpm-workspace.yaml` 明确拒绝未使用脚本；离线复装退出码 0 |
| 自动 QA 的 focus 截图仍显示 note 面板 | 1 | 改用真实标签点击并等待双 requestAnimationFrame；DOM 与截图均确认 focus 正确显示 |
| 计划日志批量补丁与旧文本不匹配 | 1 | 重新读取当前三个计划文件并按实际内容精确更新 |
| V2 冒烟测试将 `toastTimer` 误判为旧专注计时功能 | 1 | 将否定断言收紧到旧工具的具体 DOM 与函数标识 |
| V2 首轮边缘 QA：左侧坐标断言失败，隐藏时便签板仍在截图中显示 | 1 | 坐标检查增加 2px Windows 边界容差；隐藏态禁用入场动画，避免动画层覆盖 opacity |
| V3 计划批量补丁命中已变化的 V2 表格文本失败 | 1 | 重新读取三个计划文件后分段精确更新；代码未受影响 |
| 图标生成首次引用了错误的 bundled Python 子路径 | 1 | 读取工作区依赖清单后改用正确 Python，可复现生成中性图标 |
| V3 冒烟测试仍断言旧 CSS 变量名 `--body-font` | 1 | 按当前设计令牌 `--font` 修正断言后全套检查通过 |
| V3.1 便携启动器返回后首次读取不到 QA 结果 | 1 | 确认 portable 启动器早于应用子进程退出；等待子进程完成后结果完整生成且全部通过 |
| `computer-use` 获取当前便签窗口截图时应用授权超时 | 1 | 不重复等待；改用主进程真实越界 bounds + 手动释放检测路径做可重复 QA |
| 最终计划状态检查把 Markdown `**` 当作正则量词 | 1 | 改用 `-SimpleMatch`/固定字符串检查；代码、产物与 QA 不受影响 |
| V3.5 冒烟测试仍保留 0.3.1 经典黄色值断言 | 1 | 删除旧 `246,240,169` 断言，保留新的浅奶黄精确色值与设置色板断言 |
| V3.6 首次进度补丁包含多余 hunk 标记 | 1 | 修正补丁结构后分别更新计划与进度；产品代码未受影响 |
| V3.7 首次跨文件边缘预览补丁未匹配 `let windowState` | 1 | 读取 renderer 顶部真实声明后拆分为主进程与 renderer 两组精确补丁 |
| V3.8 清理旧 hover IPC 的跨文件补丁未匹配 preload 的 `Boolean(pinned)` | 1 | 读取 preload 原文，后续按每个文件的真实行拆分精确修改；主进程监视器补丁不受影响 |
| 首次 0.3.8 指针复测实例启动后立即退出 | 1 | 隔离目录仍被 Electron 单实例锁路径干扰；测试专用 `DESKTOP_NOTE_USER_DATA_DIR` 模式跳过生产单实例锁，并换全新隔离目录，正常用户启动仍保留锁 |
| 0.3.8 首次打包在沙箱网络中 EACCES | 1 | Electron 构建资源请求被网络沙箱阻止；按权限规范改为受控 `pnpm dist` 联网重试 |
| 成品测试清理时 CIM/单个 helper 报拒绝访问 | 1 | 不依赖 CIM；按已验证的“桌面便签”测试进程集合停止，随后复查进程数为 0，未影响成品或用户数据 |

## Notes
- 用户原句“可以隐藏，也可以实现”按上下文理解为可隐藏、可再次显示；实现时通过托盘和全局快捷键覆盖该需求。
- 若现有项目约束出现，将优先保持用户已有文件；当前目录为空。
- 便携版未使用商业 Authenticode 证书，首次运行可能出现 Windows SmartScreen 提示；README 与 SHA256SUMS 已提供校验说明。
- V2 会迁移 V1 的单条便签内容；专注和常用语数据不再展示，但不会主动清理旧 localStorage 键。
- 新版便携文件为 `release/小芽便签-0.2.0-x64.exe`；旧 0.1.0 文件保留作回退，但 README 和交付链接均指向新版。
- V3 将从 `sprout-notes:v2` 迁移事项正文与原排序；新时间字段默认空缺。
- V3 最终便携文件为 `release/桌面便签-0.3.0-x64.exe`；源码版与便携版 QA 均为 `passed: true`。
- 四款皮肤截图在当前 125% Windows 缩放下均为 375×250 物理像素，对应完全一致的 300×200 逻辑尺寸。
- V3.1 最终便携文件为 `release/桌面便签-0.3.1-x64.exe`；四皮肤 50%/75%/100% 实际 alpha 一致，成品 QA 为 `passed: true`。
- V3.2 最终便携文件为 `release/桌面便签-0.3.2-x64.exe`；字体、垂直居中、时间图标与完整成品 QA 均通过。
- V3.3 最终便携文件为 `release/桌面便签-0.3.3-x64.exe`；参考图导向的 UI、设置磨砂层和右侧越界隐藏均已在源码与成品中复验通过。
- V3.4 最终便携文件为 `release/桌面便签-0.3.4-x64.exe`；事项正文缩小至 13px，源码与成品完整 QA 均通过。
- V3.5 最终便携文件为 `release/桌面便签-0.3.5-x64.exe`；经典黄调整为偏白浅奶黄，源码与成品完整 QA 均通过。
- V3.6 最终便携文件为 `release/桌面便签-0.3.6-x64.exe`；时间改为行内元数据，事项 12.5px、时间 12px，源码与成品完整 QA 均通过。
- V3.7 最终便携文件为 `release/桌面便签-0.3.7-x64.exe`；日期保留日历选择、时间改为直接键入，侧边拉手支持悬浮展开和移出收回，源码与成品完整 QA 均通过。
- V3.8 最终便携文件为 `release/桌面便签-0.3.8-x64.exe`；hover 改为系统光标状态机并修复 Windows 透明表面重绘，源码与成品均完成 `26px → 300px 完整 UI → 26px` 真实指针闭环。
